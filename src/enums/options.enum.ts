export enum Currencies {
  BRL = 'BRL',
  EUR = 'EUR',
  CVE = 'CVE',
  MZN = 'MZN'
}

export enum Modes {
  NUMBER = 'number',
  CURRENCY = 'currency'
}

export enum Locales {
  BR = 'br',
  PT = 'pt'
}

export enum Negatives {
  INFORMAL = 'informal',
  FORMAL = 'formal'
}

export enum Scales {
  SHORT = 'short',
  LONG = 'long'
}

export enum Genders {
  MASCULINE = 'm',
  FEMININE = 'f'
}

export enum Decimals {
  FORMAL = 'formal',
  INFORMAL = 'informal'
}

export enum DecimalSeparators {
  COMMA = 'comma',
  DOT = 'dot'
}
