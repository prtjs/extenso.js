export enum Currencies {
  AOA = 'AOA',
  CVE = 'CVE',
  BRL = 'BRL',
  XOF = 'XOF',
  MZN = 'MZN',
  EUR = 'EUR',
  STN = 'STN',
  USD = 'USD',
  MOP = 'MOP',
}

export enum Modes {
  CURRENCY = 'currency',
  DIGIT = 'digit',
  NUMBER = 'number',
}

export enum Locales {
  BR = 'br',
  PT = 'pt',
}

export enum Scales {
  SHORT = 'short',
  LONG = 'long',
}

export enum Genders {
  MALE = 'male',
  FEMALE = 'female',
}

export enum DecimalSeparators {
  COMMA = 'comma',
  DOT = 'dot',
}
